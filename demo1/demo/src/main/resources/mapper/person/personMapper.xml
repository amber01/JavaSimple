<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 指定工作空间，要与接口名相同，源代码没有去看，猜测应该是通过"这里的namespace.下边方法的id"来定位方法的 -->
<mapper namespace="com.example.demo.mybatisDemo.mapper.PersonMapper">  <!-- 可以直接输入PersonMapper类名吗 -->

    <select id="findAll" parameterType="int" resultType="com.example.demo.mybatisDemo.entity.Person">
            select *from person;
    </select>

    <select id="findById" parameterType="int" resultType="com.example.demo.mybatisDemo.entity.Person">
        select *from person where id#{id}
    </select>

    <delete id="deleteById" parameterType="int">
        delete *from person where id#{id}
    </delete>

</mapper>